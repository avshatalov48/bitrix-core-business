import {SourceField} from '../../src/sourcefield';

describe('landing.ui.field.sourcefield', () => {
	describe('SourceField', () => {
		it('Should be a function', () => {
			assert(typeof SourceField === 'function');
		});
	});
});