<?php

namespace Bitrix\Calendar\Sync\Connection;

use Bitrix\Calendar\Core\Base\EntityMap;

/**
 * @method self add(SectionConnection $item, $key = null)
 * @method self addItems(array $items)
 * @method self updateItem(SectionConnection $item, $key)
 * @method SectionConnection getItem($key)
 * @method self getItemsByKeys(array $key)
 * @method SectionConnection[] getCollection()
 * @method SectionConnection fetch()
 */
class SectionConnectionMap extends EntityMap
{
}
