import {MessageTemplateSelector} from '../../src/message-template-selector';

describe('MessageTemplateSelector', () => {
	it('Should be a function', () => {
		assert(typeof MessageTemplateSelector === 'function');
	});
});